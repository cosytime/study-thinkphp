<?php

// +----------------------------------------------------------------------
// | JWT配置
// +----------------------------------------------------------------------

return [
    'key' => '1233998552',  // JWT密钥
    'iss' => 'HangWei',  // 签发者
    'aud' => 'bsu', // 面象的用户，可以为空
    'iat' => time(), // 签发时间
    'nbf' => time(), // JWT开始生效时间（这里表示生成后立即生效）
    'exp' => time() + 15 * 24 * 3600, // TOKEN过期时间（这里表示生成后的15天后失效）
];
